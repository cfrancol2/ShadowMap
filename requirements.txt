# ShadowMap - Dependencias del Proyecto
# ==========================================
# Sistema objetivo: Linux (Kali/Ubuntu/Debian)
# Python 3.9+
#
# Instalación completa (todas las fases):
#   PASO 1: pip install torch --index-url https://download.pytorch.org/whl/cpu
#   PASO 2: pip install -r requirements.txt
#
# ⚠️ IMPORTANTE: Ejecuta el PASO 1 PRIMERO. Si no, pip intentará descargar
#    PyTorch con CUDA (~1GB) que puede llenar el disco en VMs con poco espacio.
#
# Instalación mínima (solo dashboard + datos existentes):
#   pip install streamlit pandas numpy plotly
# ==========================================

# ==========================================
# DASHBOARD (Streamlit)
# ==========================================
streamlit>=1.28.0
plotly>=5.14.0

# ==========================================
# FASE 1 - Scraping de foros .onion
# ==========================================
# Requiere Tor corriendo localmente (SOCKS5 127.0.0.1:9050)
# Instalar Tor: sudo apt install tor
beautifulsoup4>=4.12.0
requests>=2.31.0
requests[socks]>=2.31.0
requests-tor>=1.4

# ==========================================
# FASE 2 - NLP con SecureBERT
# ==========================================
# PyTorch se instala por separado (ver instrucciones arriba).
# CPU-only (~200MB) vs CUDA (~1GB+).
torch>=2.0.0
transformers>=4.36.0
scikit-learn>=1.3.0
python-dateutil>=2.8.0
jsonlines>=4.0.0

# ==========================================
# FASE 3 - Modelo HMM
# ==========================================
hmmlearn>=0.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ==========================================
# DEPENDENCIAS COMPARTIDAS
# ==========================================
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0

# ==========================================
# INSTALACIÓN RÁPIDA POR SEPARADO (Linux)
# ==========================================
# Solo dashboard:     pip install streamlit pandas numpy plotly
# Fase 1:             sudo apt install tor && pip install beautifulsoup4 requests requests[socks] requests-tor pandas
# Fase 2 (CPU):       pip install torch --index-url https://download.pytorch.org/whl/cpu
#                     pip install transformers pandas numpy scikit-learn tqdm python-dateutil requests jsonlines
# Fase 3:             pip install hmmlearn pandas numpy matplotlib seaborn tqdm
# Desinstalar todo:   pip uninstall -y -r <(pip freeze)
